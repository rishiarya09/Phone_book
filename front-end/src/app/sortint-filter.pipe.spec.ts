import { SortintFilterPipe } from './sortint-filter.pipe';

describe('SortintFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SortintFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
